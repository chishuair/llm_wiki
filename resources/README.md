# Offline Resources

This directory is the packaging root for offline AI Case Wiki capabilities.

- `lawbase/`: bundled legal database files.
- `ocr/`: bundled PaddleOCR sidecar, models, and runtime files.
- `pdfium/`: bundled PDFium libraries.
- `docs/`: offline help documents.
