# 案件知识库：项目配置与使用说明

本文档基于当前项目实际配置文件编写，覆盖“开发配置、打包配置、插件配置、日常使用流程”。

## 1. 项目简介

本项目是基于 Tauri + React 的桌面知识库应用，当前产品名为“案件知识库”。你导入资料后，系统会自动摄入并生成结构化 Wiki，支持聊天问答、知识图谱分析和深度研究。

---

## 2. 项目关键配置（按文件）

### 2.1 `src-tauri/tauri.conf.json`

当前主要配置如下：
- `productName`: `案件知识库`
- `identifier`: `com.caseknowledge.desktop`
- `version`: `0.3.4`
- 开发模式前置命令：`npm run dev`
- 开发 URL：`http://localhost:1420`
- 构建前置命令：`npm run build`
- 前端产物目录：`../dist`
- 主窗口默认尺寸：`1200 x 800`
- 打包资源：`../lawbase-pack-full/lawbase-pack.json`、`../lawbase-pack-full/manifest.json`

使用要点：
- 如果你更换项目名/包名，优先修改 `productName` 与 `identifier`。
- 如果前端端口变化，必须同步更新 `devUrl`。
- 打包依赖 `lawbase-pack-full` 目录，缺少时可能导致构建资源不完整。

### 2.2 `package.json`

当前常用脚本：
- `npm run dev`：启动前端开发服务（Vite）
- `npm run build`：TypeScript 检查 + Vite 构建
- `npm run tauri dev`：启动桌面应用开发模式
- `npm run tauri build`：构建桌面安装包
- `npm run test`：运行单测（排除 real-llm）
- `npm run test:llm`：运行真实 LLM 测试
- `npm run lawbase:pack`：生成 lawbase 数据包

### 2.3 `extension/manifest.json`（Chrome 剪藏插件）

当前主要配置如下：
- 插件名：`LLM Wiki Clipper`
- `manifest_version`: `3`
- 本地通信权限：`http://127.0.0.1:19827/*`
- 权限：`activeTab`、`scripting`
- Popup 页面：`popup.html`

使用要点：
- 桌面端本地服务端口需与插件里 `19827` 保持一致。
- 若后续修改本地 API 地址，插件 `host_permissions` 必须同步更新。

---

## 3. 环境准备

### 3.1 直接安装（推荐）

从 GitHub Releases 下载对应系统安装包：
- macOS: `.dmg`
- Windows: `.msi`
- Linux: `.deb` 或 `.AppImage`

### 3.2 源码运行（开发者）

前置要求：
- Node.js 20+
- Rust 1.70+

在项目根目录执行：

```bash
npm install
npm run tauri dev
```

若需构建生产包：

```bash
npm run tauri build
```

---

## 4. 首次启动配置

1. 启动应用后，创建一个新项目（建议先选默认模板）。
2. 进入设置页，配置 LLM 服务：
   - Provider（例如 OpenAI、Anthropic、Google、Ollama 或自定义）
   - API Key
   - 模型名称
3. 选择界面语言（中文/英文）和上下文窗口大小（默认值可先不改）。
4. 保存设置后返回主界面。

> 建议先用一个稳定、速度较快的模型完成初始化，后续再根据质量和成本调整模型。

---

## 5. 导入资料并构建 Wiki

1. 打开“资料源”面板。
2. 导入文件或文件夹（支持 PDF、DOCX、PPTX、XLSX、Markdown、图片等）。
3. 查看“活动面板”中的摄入进度。
4. 摄入完成后，系统会自动更新：
   - `wiki/index.md`（目录）
   - `wiki/overview.md`（全局概览）
   - 各类实体/概念/资料页面

说明：
- 摄入采用队列机制，失败任务可重试。
- 未变更文件会被增量跳过（节省时间和 token）。

---

## 6. 日常使用流程

### 5.1 聊天问答

在聊天区直接提问，系统会自动检索 Wiki 相关页面后再交给 LLM 生成回答。  
适合场景：
- 让系统总结某个主题
- 对比多个概念/文档观点
- 基于已有资料生成提纲或行动建议

### 5.2 知识图谱

在图谱页查看页面之间的关系，快速发现：
- 聚类主题（社区）
- 关键桥接节点
- 可能存在的知识空白

### 5.3 审核队列

在审核页处理 LLM 标记的“需人工判断项”，常见操作：
- 创建页面
- 发起深度研究
- 跳过

### 5.4 Lint 检查

定期运行 Lint，修复结构和链接问题，保持 Wiki 健康度。

---

## 7. 可选功能

### 6.1 向量搜索（可选）

在设置中开启后，问答检索将融合语义相似度，通常可以提升召回质量。  
开启前需配置 embedding 端点、API Key 与模型。

### 6.2 深度研究（可选）

当你需要补齐知识空白时可发起 Deep Research：
1. 系统生成研究主题与搜索查询
2. 你可手动修改后确认
3. 研究结果会自动写入 Wiki 并参与后续检索

### 6.3 Chrome 网页剪藏（可选）

1. 打开 `chrome://extensions`
2. 启用开发者模式
3. 加载项目中的 `extension/` 目录
4. 在网页侧触发剪藏并自动摄入到项目

---

## 8. 目录说明

典型项目目录如下：

```text
my-wiki/
├── purpose.md
├── schema.md
├── raw/
│   ├── sources/
│   └── assets/
├── wiki/
│   ├── index.md
│   ├── log.md
│   ├── overview.md
│   ├── entities/
│   ├── concepts/
│   ├── sources/
│   ├── queries/
│   ├── synthesis/
│   └── comparisons/
└── .llm-wiki/
```

---

## 9. 常见问题

### Q1: 导入后没有生成内容？

- 先检查活动面板是否仍在排队/处理中
- 检查 LLM 配置是否有效（API Key、模型名、可用额度）
- 失败任务可直接重试

### Q2: 回答质量不稳定怎么办？

- 在 `purpose.md` 中写清你的目标、边界和关键问题
- 优化 `schema.md` 的页面分类规则
- 调整模型或增大上下文窗口

### Q3: 数据会不会丢？

- 项目数据本地持久化保存
- 建议定期备份你的项目目录（尤其是 `raw/`、`wiki/`、`.llm-wiki/`）

### Q4: 为什么我打包后缺少法律数据？

- 请确认存在 `lawbase-pack-full/lawbase-pack.json` 和 `lawbase-pack-full/manifest.json`
- 如缺失，可先执行 `npm run lawbase:pack` 生成资源，再重新打包

---

## 10. 推荐实践

- 先用小规模资料验证流程，再大批量导入
- 每周处理一次审核队列并运行一次 Lint
- 重要回答及时“保存到 Wiki”，让知识沉淀可复用
- 保持 `purpose.md` 持续更新，让系统长期贴合你的目标

